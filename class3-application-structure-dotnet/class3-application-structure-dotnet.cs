/*

A .NET application is represent with classes

The classes is grouped in namespaces (like packages in Java).
Namespace is LOGICAL grouping of related classes. (Ex: Entities, Services, Repositories)

The namespace is grouped in Assembly.

A Assembly is a DLL or EXE file.

Assembly is PHYSICAL grouping of related classes(namespace) 

(Ex: Financial with namespace entities, services and repositories and
graphics with 3dgraphics, 2dgraphics and utilities)

A Application is Grouping related assemblies

Ex: A system of a commercial

 In Visual Studio: Application is a solution in VS and inside a application we have assemblies that is projects in VS

*/